# GEMINI_API_KEY: Required for Gemini AI API calls.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# AUTH_SECRET: Secret used by NextAuth to encrypt tokens
AUTH_SECRET="my-super-secret-auth-key"

# Supabase Postgres (Prisma)
# DATABASE_URL: use o "Connection string" com pooler (porta 6543) para runtime da aplicação.
# DIRECT_URL: use a conexão direta (porta 5432) para migrations do Prisma.

# Connect to Supabase via connection pooling
